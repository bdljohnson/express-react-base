{
  "name": "todo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "watch-dev": "parcel watch src/index.html --public-url ./",
    "server-dev": "node app.js",
    "dev": "npm-run-all -p -r watch-dev server-dev",
    "build": "parcel build src/index.html --out-dir public/ --public-url ./",
    "start": "parcel build src/index.html --out-dir public/ --public-url ./ && node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.16.3",
    "querystring": "^0.2.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "request": "^2.85.0"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-react": "^6.24.1",
    "node-sass": "^4.7.2",
    "npm-run-all": "^4.1.2",
    "parcel-bundler": "^1.6.2"
  }
}
